/// Enum to identify the environment of the app.
///
/// Never change the enviriment at runtime.
enum Enviroment {
  /// Development environment.
  development,

  /// Staging environment.
  staging,

  /// Production environment.
  production,
}
